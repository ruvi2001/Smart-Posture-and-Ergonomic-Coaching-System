document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('chat-page-form');const input=document.getElementById('chat-page-input');const messages=document.getElementById('chat-page-messages');form.onsubmit=async(e)=>{e.preventDefault();const text=input.value.trim();if(!text)return;messages.insertAdjacentHTML('beforeend',`<div class="float-user">${escapeHtml(text)}</div>`);input.value='';const wait=document.createElement('div');wait.className='float-bot';wait.textContent='Thinking...';messages.appendChild(wait);const data=await askPostureAI(text);wait.innerHTML=escapeHtml(data.error?data.error:data.reply).replace(/\n/g,'<br>');messages.scrollTop=messages.scrollHeight;};});
